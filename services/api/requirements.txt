# Web framework + ASGI server
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Config — reads .env.local into typed Python classes
pydantic-settings==2.2.1
pydantic==2.7.1

# Database — async ORM + driver (migrations run in Supabase SQL editor, not alembic locally)
sqlalchemy[asyncio]==2.0.30
asyncpg==0.30.0
alembic==1.13.1

# Auth — Firebase JWT verification
firebase-admin==6.5.0
